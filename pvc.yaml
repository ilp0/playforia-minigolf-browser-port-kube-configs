apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minigolf-logs
  namespace: minigolf
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
